<%
/**
 * Clinical band indicator. `level` is the ONLY route by which saturated
 * colour enters the interface — never use this for decoration.
 * @param {'calm'|'watch'|'act'} level
 * @param {string} label
 */
%><span class="badge badge--<%= level %>"><%= label %></span>
