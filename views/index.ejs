<%
/**
 * Home page.
 *
 * Deliberately thin: all copy and section order live in
 * `content/<locale>/pages/home.js`. Changing the page means editing content,
 * not this file.
 */
const page = t.pages.home;
%><%- include('partials/base-head', { pageTitle: page.meta.title, pageDesc: page.meta.description, canonical: '/' }) %>

<%- include('section-renderer', { sections: page.sections }) %>

<%- include('partials/base-foot') %>
