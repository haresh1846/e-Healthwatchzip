<%- include('partials/header', {
  pageTitle: 'BMD Records',
  activeTab: 'bmd',
  userCount: locals.userCount || 0,
  orderCount: locals.orderCount || 0,
  resultCount: locals.resultCount || 0,
  bmdCount: records.length
}) %>

<div class="adm-body">

  <div class="adm-stats">
    <div class="adm-stat">
      <div class="adm-stat-label">BMD Records (in progress)</div>
      <div class="adm-stat-value"><%= records.length %></div>
    </div>
  </div>

  <div class="adm-section-header">
    <h1 class="adm-section-title">BMD Records</h1>
    <div class="adm-section-spacer"></div>
    <div class="adm-search">
      <svg xmlns="http://www.w3.org/2000/svg" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
      <input type="text" id="search-input" placeholder="Search by name…" oninput="filterTable()" autocomplete="off">
    </div>
    <a href="/admin/bmd?export=csv" class="adm-export">
      <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>
      Export CSV
    </a>
  </div>

  <div class="adm-table-wrap">
    <table class="adm-table" id="data-table">
      <thead>
        <tr>
          <th>#</th>
          <th>Patient Name</th>
          <th>Age</th>
          <th>Height (cm)</th>
          <th>Weight (kg)</th>
          <th>HAL</th>
          <th>NSA (°)</th>
          <th class="hide-mobile">Session ID</th>
        </tr>
      </thead>
      <tbody>
        <% if (records.length === 0) { %>
          <tr><td colspan="8" class="adm-empty">No pending BMD records. Records are deleted after results are shown to the clinic user.</td></tr>
        <% } %>
        <% records.forEach(function(r) { %>
        <tr>
          <td class="mono"><%= r.id %></td>
          <td><strong><%= r.name || '—' %></strong></td>
          <td><%= r.age || '—' %></td>
          <td><%= r.height || '—' %></td>
          <td><%= r.weight || '—' %></td>
          <td><%= r.hal || '—' %></td>
          <td><%= r.nsa || '—' %></td>
          <td class="mono hide-mobile" style="font-size:11px;max-width:120px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;"><%= r.guid || '—' %></td>
        </tr>
        <% }); %>
      </tbody>
    </table>
  </div>

  <p style="margin-top:16px;font-size:12px;color:#94A3B8;">
    BMD records are deleted immediately after the clinic result is computed. Only records from incomplete sessions appear here.
  </p>
</div>

<script>
function filterTable() {
  var q = document.getElementById('search-input').value.toLowerCase();
  document.querySelectorAll('#data-table tbody tr').forEach(function(row) {
    row.style.display = row.textContent.toLowerCase().indexOf(q) > -1 ? '' : 'none';
  });
}
</script>

<%- include('partials/footer') %>
